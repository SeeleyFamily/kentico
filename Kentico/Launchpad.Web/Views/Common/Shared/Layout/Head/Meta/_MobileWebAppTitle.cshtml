@*
    Built with Common Launchpad 2.0.2
*@

@model BaseViewModel

@if (!string.IsNullOrWhiteSpace(Model.Metadata.MobileWebAppTitle))
{
    <meta name="apple-mobile-web-app-title" content="@Model.Metadata.MobileWebAppTitle">
}