@*
    Built with Common Launchpad 2.0.2
*@

@model BaseViewModel

@if (!string.IsNullOrWhiteSpace(Model.Settings.GoogleSiteVerificationCode))
{
    <meta name="google-site-verification" content="@Model.Settings.GoogleSiteVerificationCode" />
}